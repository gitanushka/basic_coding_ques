#include <bits/stdc++.h>
using namespace std;

int Sum(int n){
    if(n==1){
        return 1;
    }
  return n + Sum(n-1);   
}
int main() {
    // Write C++ code here
    int n;
    cout<<"Enter number till which you want sum of natural no."<<endl;
    cin>>n;
    cout<< Sum(n);
    return 0;
}
