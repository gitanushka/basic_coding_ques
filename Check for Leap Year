#include <bits/stdc++.h>
using namespace std;

void leapYear(int year){
    
   if((year%4==0 && year%100!=0) || year%400==0){
       cout<<"It is leap year";
   }
   else{
       cout<<"Not a leap year";
   }
}
int main() {
    // Write C++ code here
    int year;
    cin >> year;
    leapYear(year);

    return 0;
}
